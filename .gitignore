# second checkout used by workflows / local test runs
putzii-app/

# dropii CLI: real config holds key + PAT — NEVER commit it
dropii.conf
# generated QR artifacts: putzii-zugang-*.png embeds FULL credentials
putzii-zugang-*.png
putzii-checkin-*.png
putzii-qr-sheet.html
# the built binary (anchored: a bare "dropii" would also ignore cmd/dropii/)
/dropii

# local build artifacts
*.test
dist/

# generated parity fixtures (CI regenerates them)
internal/dropcrypto/testdata/
internal/wire/testdata/
